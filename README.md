# aicoding
learing to implement a browser agent

```
import sys
import html
from pathlib import Path
from urllib.parse import quote
```
This bit of the code uses `import` for importing libraries (code that other people havee written and I could use). They provide new functionalities.

